-- P34: 토큰 값만 다시 발급한 시각.
--
-- 값을 바꿔도 이름·범위·만료는 그대로 두는 "재발급"을 위해서다. 이 시각이 없으면
-- 목록의 "발급 …"이 지금 살아 있는 값이 아니라 **처음 만든 값**의 시각을 가리키게
-- 된다 — 어제 재발급한 토큰이 석 달 전에 만든 것처럼 보이면, 클라이언트에 넣어 둔
-- 값이 최신인지 아무도 확신할 수 없다.
--
-- created_at은 그대로 둔다. "이 토큰을 언제부터 쓰기 시작했는가"와 "지금 값이 언제
-- 나온 것인가"는 다른 사실이고, 둘 다 쓸모가 있다.

ALTER TABLE api_tokens ADD COLUMN rotated_at TEXT;
