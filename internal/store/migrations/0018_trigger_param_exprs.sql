-- P14 보강: 트리거 파라미터를 실행 시점에 계산한다.
--
-- 지금까지 트리거는 고정값만 넘길 수 있었다. 그런데 자동 실행에서 넘기고 싶은 값은
-- 대개 그때그때 다르다 — "방금 터진 이벤트의 커넥션", "오늘 날짜", "심각도에 따라
-- 다른 대상". 고정값만으로는 이벤트마다 트리거를 하나씩 만들어야 한다.
--
-- params와 나눠 두는 이유: 한 칸에 섞으면 "이 값이 글자 그대로인가 식인가"를
-- 값 모양으로 짐작해야 한다. 식은 식이라고 적어 두는 편이 안전하다.
-- 같은 키가 양쪽에 있으면 식이 이긴다(사람이 나중에 식으로 바꾼 것이므로).
ALTER TABLE macro_triggers ADD COLUMN param_exprs TEXT NOT NULL DEFAULT '{}';
